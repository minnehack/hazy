<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.3/dist/confetti.browser.min.js"></script>
<main>
    <h1>Thank you!</h1>
    <p>Thanks for registering for MinneHack. We'll send you an email from acmumn@gmail.com with more registration info.</p>
    <p>If you don't see anything right away, check your spam box.</p>
    <p>(Or our mail service could be falling over. In that case, wait a bit.)</p>
    <canvas id="confetti"></canvas>
    <script defer>
        document.addEventListener('DOMContentLoaded', () => {
            const canvas = document.getElementById('confetti');
            const myConfetti = confetti.create(canvas, {
                resize: true,
                useWorker: true,
            })
            myConfetti()
        });
    </script>
</main>